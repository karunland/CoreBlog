@{
    ViewData["Title"] = "add";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

@model WebUI.Controllers.Class1

<form method="post" href="/editProfile">
    <div class="form-control">
        <label class="form-label" asp-for="@Model.Name"> full name </label>
        <input class="form-label" asp-for="@Model.Name" placeholder=" name" />
        <input hidden asp-for="@Model.Id" />
    </div>
    <div class=" form-control">
        <button class="btn btn-primary">Save</button>
    </div>
</form>
